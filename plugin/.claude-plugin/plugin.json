{
  "name": "tuivio-tui-dev",
  "version": "2.2.0",
  "description": "TUI development plugin with visual feedback loop",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "command -v tmux >/dev/null || echo '[Tuivio] tmux not found. Install it: brew install tmux (macOS) or apt install tmux (Linux)'"
          }
        ]
      }
    ]
  }
}